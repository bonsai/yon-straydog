# 状態管理とテキスト表示フローの実装

## 概要
ゲーム全体の状態管理（GamePhase）とテキスト表示の流れを実装する。

## タスク
- [ ] `game-state.ts` のフェーズ遷移を完成させる（intro → puzzle → hub → play → story → complete）
- [ ] テキスト表示UI（アドベンチャー形式のオーバーレイ）を作成する
- [ ] 各フェーズに応じた適切な画面表示を制御する
- [ ] ユーザーのタップ/クリック入力を待つ処理を実装する

## 備考
現在は `main.ts`, `adventure.ts`, `story-mode.ts` に分散している。一元管理されたフローが必要。
