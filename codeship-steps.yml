- type: serial
  steps:
    - name: Install dependencies
      service: app
      command: npm install

    - name: Build
      service: app
      command: npm run build

    - name: Deploy to Surge
      service: app
      command: npx surge dist/ straydog.surge.sh --token $SURGE_TOKEN
